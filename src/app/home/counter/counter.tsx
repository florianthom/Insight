import React from "react";

export interface Props {}

export const Counter: React.FC<Props> = (props: Props) => {
    return (
        <div>
            <div>Counter.tsx</div>
        </div>
    );
};
